# Copy this file to .env.local and fill in your values

# Backend API URL
VITE_CATEN_BASE_URL=http://localhost:8000

# Google OAuth Client ID (required)
VITE_GOOGLE_CLIENT_ID=

# OAuth Redirect URI (optional, defaults to window.location.origin)
VITE_REDIRECT_URI=

# Paddle Configuration
# Get client token from: Paddle > Developer Tools > Authentication > Client-side tokens
# Environment: 'sandbox' for testing, 'production' for live
VITE_PADDLE_CLIENT_TOKEN=
VITE_PADDLE_ENVIRONMENT=sandbox

# Paddle Price IDs (get from Paddle Dashboard > Catalog > Prices)
VITE_PADDLE_PRICE_PRO_MONTHLY=
VITE_PADDLE_PRICE_PRO_YEARLY=
VITE_PADDLE_PRICE_ULTRA_MONTHLY=
VITE_PADDLE_PRICE_ULTRA_YEARLY=

# Paddle Discount IDs (optional - each discount is restricted to its specific price)
VITE_PADDLE_DISCOUNT_PRO_MONTHLY=
VITE_PADDLE_DISCOUNT_PRO_YEARLY=
VITE_PADDLE_DISCOUNT_ULTRA_MONTHLY=
VITE_PADDLE_DISCOUNT_ULTRA_YEARLY=
